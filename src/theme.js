import { createMuiTheme } from "@material-ui/core/styles";

// Only here for deployment
const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#084f1f"
    },
    secondary: {
      main: "#084f1f"
    },
  },
});

export default theme;
